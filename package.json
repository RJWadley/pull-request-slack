{
  "name": "pull-request-slack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm i && nodemon --watch 'src/**/*' -e ts,json --exitcrash --exec 'tsc --outDir build && node build/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/rest": "^20.0.1",
    "@octokit/types": "^11.1.0",
    "@slack/bolt": "^3.13.3",
    "dotenv": "^16.3.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^20.4.8",
    "nodemon": "^3.0.1"
  }
}
